'use client';

import React from 'react';
import { BaseSection } from '@/components/ui/BaseSection';
import { Logo } from '@/components/common/Logo';
import { cn } from '@/lib/utils';
import { layout } from '@/lib/theme/tokens/layout';
import { typography } from '@/lib/theme/tokens/typography';
import { colors } from '@/lib/theme/tokens/colors';

export function HeroSection() {
  return (
    <BaseSection
      id="hero"
      className={cn(
        layout.section?.base,
        'relative min-h-[100svh] overflow-hidden px-6 md:px-8 lg:px-10 flex items-center'
      )}
      ariaLabel="Hero Section"
    >
      {/* Logo - placeras absolut inom hero men påverkar inte layout */}
      <div className="absolute top-6 left-6">
        <Logo className={cn(typography.logo, colors.text.logo.text)} />
      </div>

      {/* Innehåll i två kolumner */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 w-full max-w-7xl mx-auto items-center">
        {/* Textsektion */}
        <div className="space-y-6">
          <h1
            className={cn(
              typography.heading.h1.replace('font-bold', 'font-normal'),
              colors.text.primary,
              'leading-tight tracking-tight'
            )}
          >
            What if growing your business didn’t mean more work?
          </h1>
          <p
            className={cn(
              typography.text.xl,
              typography.weight.normal,
              colors.text.secondary,
              'leading-relaxed max-w-xl'
            )}
          >
            Let AI Handle 80% of Your Work—Effortlessly. Meet Your 24/7 Digital Employee—For Free.
          </p>
        </div>

        {/* Widget */}
        <div className="flex justify-center items-center w-full">
          <div className="relative w-full max-w-[440px] max-h-[480px] sm:max-h-[520px] h-full">
            <elevenlabs-convai
              agent-id="TDgRNcUoUC1GHVKK0bHH"
              mode="embedded"
              style={{
                height: '100%',
                width: '100%',
                background: 'transparent',
                borderRadius: '12px',
                position: 'relative',
              }}
            />
          </div>
        </div>
      </div>
    </BaseSection>
  );
}
