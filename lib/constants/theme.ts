import type { ThemeSection } from "@/lib/utils/theme-validator";

export const COMMON_THEME_SECTIONS: ThemeSection[] = [
  "typography",
  "spacing",
  "colors",
  "effects",
  "radius"
];

export const MINIMAL_THEME_SECTIONS: ThemeSection[] = [
  "typography",
  "colors",
  "spacing"
]; 